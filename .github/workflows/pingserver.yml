name: Keep Alive
on:
  schedule:
    - cron: '*/5 * * * *' # Runs every 5 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl https://zuzu-frontend.onrender.com/
