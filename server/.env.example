# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_service_role_key_here

# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys
ZUZU_OPENROUTER_KEY=sk-or-v1-your-openrouter-key-here

# Server Configuration
PORT=5000

# Frontend URL (used in email links for password reset)
FRONTEND_URL=http://localhost:3000

# Production Frontend URL (optional - automatically added to CORS allowed origins)
PRODUCTION_FRONTEND_URL=https://your-production-frontend-url.com

# CORS Configuration (optional - comma-separated list for additional origins)
# Leave empty to use defaults (localhost:3000 + PRODUCTION_FRONTEND_URL)
ALLOWED_ORIGINS=

# JWT Configuration
# Generate strong random secrets for production
JWT_ACCESS_SECRET=your-access-secret-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-change-in-production

# CSRF Protection
# Generate a strong random secret for production
CSRF_SECRET=your-csrf-secret-change-in-production

# Development Auto-Login (local environment only)
# Set to a valid user email to enable automatic login in local development
# ONLY works when running on localhost - ignored in production
TEST_USER_EMAIL=

# SMTP Email Configuration (for Mailgun or other SMTP providers)
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=ZuZu Auth <noreply@yourdomain.com>
EMAIL_REPLY_TO=support@yourdomain.com
