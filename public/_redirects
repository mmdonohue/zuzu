# Serve static files as-is (no redirect)
/assets/* 200
/images/* 200
/*.js 200
/*.css 200
/*.wasm 200
/*.json 200
/*.ico 200
/*.png 200
/*.jpg 200
/*.jpeg 200
/*.JPEG 200
/*.gif 200
/*.svg 200

# Catch-all for React Router (must be last)
/* /index.html 200
